import { styled } from "styled-components";

const Wrapper=styled.section`

.quiz-result {
  text-align: center;

  h2 {
    font-size: 2rem;
    color: #333;
    margin-bottom: 20px;
  }

  p {
    font-size: 1.5rem;
    color: #007bff;
  }
}

`
export default Wrapper;
